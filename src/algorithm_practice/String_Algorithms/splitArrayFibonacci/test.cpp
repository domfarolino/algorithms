// Example program
#include <iostream>
#include <string>
#include <string>
#include <climits>

void fn(long num) {
  std::cout << num << std::endl;
}

int main() {
  int x = INT_MAX, y = INT_MAX;
  fn(x + y);
}
